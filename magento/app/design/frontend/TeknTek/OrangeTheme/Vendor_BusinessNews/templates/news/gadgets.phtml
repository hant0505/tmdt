<?php
/**
 * gadgets.phtml — Section "What's your go-to gadget for travel?"
 *
 * Block: Vendor\BusinessNews\Block\BusinessNews
 * Hiển thị 4 bài từ vị trí 5-8 của RSS feed dưới dạng card ngang
 * Có ảnh lớn + tiêu đề + link "View all"
 *
 * @var \Vendor\BusinessNews\Block\BusinessNews $block
 */
$gadgets = $block->getGadgets(); // bài từ offset 4
?>
<section class="bn-gadgets">

    <!-- Header section -->
    <div class="bn-gadgets__header">
        <h4 class="bn-gadgets__title">What's your go-to gadget for travel?</h4>
        <a href="#" class="bn-gadgets__view-all">
            View all
            <svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" class="bn-icon-arrow">
                <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
        </a>
    </div>

    <!-- Grid gadget cards -->
    <?php if (!empty($gadgets)): ?>
        <div class="bn-gadgets__grid">
            <?php foreach ($gadgets as $gadget): ?>
                <article class="bn-gadget-card">
                    <?php $link = $gadget['link'] ?? '#'; ?>
                    <a href="<?= htmlspecialchars($link, ENT_QUOTES, 'UTF-8') ?>"
                       class="bn-gadget-card__link"
                       target="_blank"
                       rel="noopener noreferrer">

                        <!-- Ảnh -->
                        <div class="bn-gadget-card__thumb-wrap">
                            <img
                                src="<?= htmlspecialchars($gadget['image'] ?? 'https://via.placeholder.com/300x200', ENT_QUOTES, 'UTF-8') ?>"
                                alt="<?= htmlspecialchars($gadget['title'] ?? '', ENT_QUOTES, 'UTF-8') ?>"
                                class="bn-gadget-card__thumb"
                                loading="lazy"
                                onerror="this.src='https://via.placeholder.com/300x200?text=No+Image'"
                            />
                            <!-- Date badge -->
                            <span class="bn-gadget-card__badge">
                                <?= htmlspecialchars($gadget['date'] ?? '', ENT_QUOTES, 'UTF-8') ?>
                            </span>
                        </div>

                        <!-- Body -->
                        <div class="bn-gadget-card__body">
                            <h5 class="bn-gadget-card__title">
                                <?= htmlspecialchars($gadget['title'] ?? '', ENT_QUOTES, 'UTF-8') ?>
                            </h5>
                            <p class="bn-gadget-card__desc">
                                <?= htmlspecialchars($gadget['description'] ?? '', ENT_QUOTES, 'UTF-8') ?>
                            </p>
                            <span class="bn-gadget-card__readmore">
                                <svg class="bn-icon-chevron" viewBox="0 0 16 16" fill="none">
                                    <path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                                </svg>
                                Xem chi tiết
                            </span>
                        </div>

                    </a>
                </article>
            <?php endforeach; ?>
        </div>
    <?php else: ?>
        <p class="bn-gadgets__empty">Chưa có nội dung.</p>
    <?php endif; ?>

</section>
